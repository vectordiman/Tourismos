export interface Sender {
  username: string;
  token: string;
  photoUrl: string;
  photoId: number;
  name: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  role: string;
  valid: boolean;
}
